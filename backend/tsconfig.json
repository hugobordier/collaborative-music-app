{
  "compilerOptions": {
    "target": "ES6", // Compile le code vers JavaScript ES6.
    "module": "commonjs", // Utilise le système de modules CommonJS (utilisé par Node.js).
    "outDir": "./dist", // Place les fichiers compilés dans le dossier 'dist'.
    "rootDir": "./src", // Les fichiers source sont situés dans le dossier 'src'.
    "strict": true, // Active toutes les options de vérification strictes.
    "esModuleInterop": true, // Assure la compatibilité entre les modules ES et CommonJS.
    "skipLibCheck": true, // Désactive la vérification des types dans les fichiers de définition des bibliothèques.
    "paths": {
      "routes/*": ["routes/*"],
      "controllers/*": ["controllers/*"],
      "models/*": ["models/*"],
      "services/*": ["services/*"],
      "utils/*": ["utils/*"]
    }
  },
  "include": ["src"], // Inclut tous les fichiers dans le dossier 'src' pour la compilation.
  "exclude": ["node_modules"] // Exclut le dossier 'node_modules' de la compilation.
}